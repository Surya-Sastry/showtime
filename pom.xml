<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.showtime</groupId>
  <artifactId>showtime-parent</artifactId>
  <version>0.1.0</version>
  <packaging>pom</packaging>

  <modules>
    <module>contracts</module>
    <module>identity-service</module>
    <module>catalog-service</module>
    <module>booking-service</module>
    <module>payment-service</module>
    <module>gateway</module>
  </modules>

  <properties>
    <java.version>21</java.version>
    <maven.compiler.release>21</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

    <spring-boot.version>3.5.16</spring-boot.version>
    <spring-cloud.version>2025.1.3</spring-cloud.version>
    <grpc.version>1.84.0</grpc.version>
    <protobuf.version>4.36.1</protobuf.version>
    <protobuf-plugin.version>0.6.1</protobuf-plugin.version>
    <os-plugin.version>1.7.1</os-plugin.version>
    <grpc-spring-boot-starter.version>3.1.0.RELEASE</grpc-spring-boot-starter.version>
    <resilience4j.version>2.4.0</resilience4j.version>
    <testcontainers.version>2.0.5</testcontainers.version>
    <flyway.version>13.6.0</flyway.version>
    <jjwt.version>0.13.0</jjwt.version>
    <opentelemetry.version>1.66.0</opentelemetry.version>
    <opentelemetry-instrumentation.version>2.31.1</opentelemetry-instrumentation.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <!-- Declared before spring-boot-dependencies on purpose: BOM entries
           are resolved first-declaration-wins, and spring-boot-dependencies
           3.5.16 transitively imports a micrometer-tracing-bom that pins
           opentelemetry-api to 1.49.0 — older than what
           opentelemetry-instrumentation-bom:2.31.1 (below) actually needs
           (~1.65.0), which breaks at runtime with NoSuchMethodError
           (GlobalOpenTelemetry.getOrNoop() didn't exist yet in 1.49.0).
           Declaring our own opentelemetry-bom/-instrumentation-bom pins
           first makes them win over Spring Boot's older transitive ones. -->
      <dependency>
        <groupId>io.opentelemetry</groupId>
        <artifactId>opentelemetry-bom</artifactId>
        <version>${opentelemetry.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>io.opentelemetry.instrumentation</groupId>
        <artifactId>opentelemetry-instrumentation-bom</artifactId>
        <version>${opentelemetry-instrumentation.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <!-- Declared before spring-cloud-dependencies on purpose: BOM entries
           are resolved first-declaration-wins, and spring-cloud-dependencies
           2025.1.3 pulls in spring-cloud-gateway-dependencies 5.0.3 by
           default, whose gateway-server-webflux jar is compiled against
           Spring Boot 4's relocated autoconfigure packages
           (org.springframework.boot.web.server.autoconfigure.ServerProperties)
           and fails NoClassDefFoundError on our Spring Boot 3.5.x. Pinning to
           the last Boot-3.x-compatible line here makes this import win. -->
      <dependency>
        <groupId>org.springframework.cloud</groupId>
        <artifactId>spring-cloud-gateway-dependencies</artifactId>
        <version>4.3.5</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <!-- spring-cloud-gateway-dependencies:4.3.5 itself depends on
           spring-cloud-starter/-context/-commons without managing their
           versions, so spring-cloud-dependencies:2025.1.3's default (5.0.3,
           also Boot-4-only) would otherwise win. Pin to the version the
           4.3.5 gateway starter's own POM was actually built and tested
           against. -->
      <dependency>
        <groupId>org.springframework.cloud</groupId>
        <artifactId>spring-cloud-starter</artifactId>
        <version>4.3.3</version>
      </dependency>
      <dependency>
        <groupId>org.springframework.cloud</groupId>
        <artifactId>spring-cloud-context</artifactId>
        <version>4.3.3</version>
      </dependency>
      <dependency>
        <groupId>org.springframework.cloud</groupId>
        <artifactId>spring-cloud-commons</artifactId>
        <version>4.3.3</version>
      </dependency>
      <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-dependencies</artifactId>
        <version>${spring-boot.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>org.springframework.cloud</groupId>
        <artifactId>spring-cloud-dependencies</artifactId>
        <version>${spring-cloud.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>io.grpc</groupId>
        <artifactId>grpc-bom</artifactId>
        <version>${grpc.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>org.testcontainers</groupId>
        <artifactId>testcontainers-bom</artifactId>
        <version>${testcontainers.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>com.showtime</groupId>
        <artifactId>contracts</artifactId>
        <version>${project.version}</version>
      </dependency>

      <dependency>
        <groupId>com.google.protobuf</groupId>
        <artifactId>protobuf-java</artifactId>
        <version>${protobuf.version}</version>
      </dependency>
      <dependency>
        <groupId>net.devh</groupId>
        <artifactId>grpc-server-spring-boot-starter</artifactId>
        <version>${grpc-spring-boot-starter.version}</version>
      </dependency>
      <dependency>
        <groupId>net.devh</groupId>
        <artifactId>grpc-client-spring-boot-starter</artifactId>
        <version>${grpc-spring-boot-starter.version}</version>
      </dependency>
      <dependency>
        <groupId>io.github.resilience4j</groupId>
        <artifactId>resilience4j-spring-boot3</artifactId>
        <version>${resilience4j.version}</version>
      </dependency>
      <dependency>
        <groupId>org.flywaydb</groupId>
        <artifactId>flyway-core</artifactId>
        <version>${flyway.version}</version>
      </dependency>
      <dependency>
        <groupId>org.flywaydb</groupId>
        <artifactId>flyway-database-postgresql</artifactId>
        <version>${flyway.version}</version>
      </dependency>
      <dependency>
        <groupId>io.jsonwebtoken</groupId>
        <artifactId>jjwt-api</artifactId>
        <version>${jjwt.version}</version>
      </dependency>
      <dependency>
        <groupId>io.jsonwebtoken</groupId>
        <artifactId>jjwt-impl</artifactId>
        <version>${jjwt.version}</version>
        <scope>runtime</scope>
      </dependency>
      <dependency>
        <groupId>io.jsonwebtoken</groupId>
        <artifactId>jjwt-jackson</artifactId>
        <version>${jjwt.version}</version>
        <scope>runtime</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <build>
    <pluginManagement>
      <plugins>
        <!-- Unlike spring-boot-starter-parent, a plain custom parent POM
             does not bind the repackage goal to the package phase by
             default. Without this <executions> block every module's `mvn
             package` produces a plain, non-executable jar (no Main-Class
             manifest entry) that fails with "no main manifest attribute"
             when run with `java -jar`. -->
        <plugin>
          <groupId>org.springframework.boot</groupId>
          <artifactId>spring-boot-maven-plugin</artifactId>
          <version>${spring-boot.version}</version>
          <executions>
            <execution>
              <goals>
                <goal>repackage</goal>
              </goals>
            </execution>
          </executions>
        </plugin>
        <plugin>
          <groupId>kr.motd.maven</groupId>
          <artifactId>os-maven-plugin</artifactId>
          <version>${os-plugin.version}</version>
        </plugin>
        <plugin>
          <groupId>org.xolstice.maven.plugins</groupId>
          <artifactId>protobuf-maven-plugin</artifactId>
          <version>${protobuf-plugin.version}</version>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>
</project>
